# DevOps Junior — Virtualization & Containers

## 1. В чём разница между виртуализацией и контейнеризацией?

### Виртуализация

* используется гипервизор (KVM, VMware)
* каждая VM имеет собственную ОС
* высокий overhead по ресурсам

### Контейнеризация

* общее ядро ОС
* изоляция через namespaces и cgroups
* быстрый старт и меньший размер

👉 Контейнеры эффективнее для микросервисов.

---

## 2. Какие преимущества контейнеров?

* быстрый запуск
* одинаковая среда dev/prod
* лёгкое масштабирование
* удобство CI/CD

---

## 3. Что такое Docker?

Docker — платформа для создания, доставки и запуска контейнеров.

Основные компоненты:

* Docker Engine
* Docker Image
* Docker Container

---

## 4. Docker image и Docker container — в чём разница?

* **Image** — шаблон (read-only)
* **Container** — запущенный image

Один image → много контейнеров.

---

## 5. Что такое Dockerfile?

Dockerfile — инструкция по сборке image.

### Пример:

```dockerfile
FROM nginx:alpine
COPY . /usr/share/nginx/html
```

---

## 6. Что такое image layers?

Image состоит из слоёв:

* каждый слой — отдельное изменение
* слои кешируются

### Почему важно:

* меньше размер image
* быстрее сборка

---

## 7. Как пробросить порт контейнера?

Пример: 80 в контейнере → 8081 на хосте

```bash
docker run -p 8081:80 nginx
```

---

## 8. Как контейнеры ограничивают ресурсы?

Используются **cgroups**:

* CPU
* memory
* IO

Пример:

```bash
docker run --memory=512m --cpus=1 nginx
```

---

## 9. Почему Docker-контейнер может активно использовать swap?

Причины:

* нет лимита памяти
* высокая нагрузка

Решения:

* задать memory limit
* оптимизировать приложение

---

## 10. Что такое volume?

Volume — механизм хранения данных вне контейнера.

Используется для:

* сохранения данных
* совместного доступа

---

## 11. Docker volumes vs bind mounts

| Volume             | Bind mount       |
| ------------------ | ---------------- |
| Управляется Docker | Прямой путь      |
| Переносимый        | Зависит от хоста |

---

## 12. Что такое docker-compose?

Docker Compose — инструмент для запуска нескольких контейнеров.

### Используется для:

* локальной разработки
* тестирования

---

## 13. PID 1 в контейнере

PID 1 — главный процесс контейнера.

Он:

* принимает сигналы
* отвечает за корректное завершение

---

## 14. Как посмотреть логи контейнера?

```bash
docker logs container_name
```

---

## 15. Как зайти внутрь контейнера?

```bash
docker exec -it container_name sh
```

---

## 16. Основные команды Docker

```bash
docker ps
docker images
docker build
docker run
docker stop
```

---

## 17. Что важно сказать на собеседовании?

* контейнеры легче VM
* image ≠ container
* обязательно упомянуть resource limits

---

✅ **Раздел Junior — Virtualization & Containers завершён**
